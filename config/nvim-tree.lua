local M = {
  git = {
    enable = true,
    ignore = false,
  },
  renderer = {
    highlight_git = true,
    highlight_opened_files = "name",

    icons = {
      show = {
        git = true,
      },
    },
  },
}

return M
